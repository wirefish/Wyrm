// Core prototype definitions.

def avatar: avatar {
    size = 'medium
}

def npc: creature {
    when talk(actor, self, topic) {
        tell(self, actor) |
            {self:D} has nothing to say to you.
    }
}
