// Core prototype definitions.

defrace human {
    brief = "a human"
}

def avatar: avatar {
    size = 'medium
}

def npc: creature {
    when talk(actor, self, topic) {
        show(actor) "{self:D} has nothing to say to you."
    }
}

def entry_doorway: portal {
    brief = "a doorway"
}
